<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- MyBatis Connection Check -->
<mapper namespace = "com.board.board.mappers.CheckTimeMapper">

    <!-- CheckTimeMapper id = "getTime" -->
    <select id="getTime">
        select now()
    </select>
    
</mapper>